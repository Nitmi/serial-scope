name: release

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: serial-scope-windows-x86_64
            binary_path: target/release/serial-scope.exe
            package_path: dist/serial-scope-windows-x86_64.zip
          - os: ubuntu-latest
            artifact_name: serial-scope-linux-x86_64
            binary_path: target/release/serial-scope
            package_path: dist/serial-scope-linux-x86_64.tar.gz

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libudev-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libx11-dev \
            libxi-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libgl1-mesa-dev \
            libasound2-dev \
            pkg-config

      - name: Build release
        run: cargo build --release

      - name: Package Windows bundle
        if: runner.os == 'Windows'
        shell: cmd
        run: packaging\windows\package-windows.bat ${{ github.ref_name }}

      - name: Package Linux bundle
        if: runner.os == 'Linux'
        run: bash packaging/linux/package-linux.sh ${{ github.ref_name }}

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.package_path }}
          if-no-files-found: error

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.package_path }}
          fail_on_unmatched_files: true
          generate_release_notes: true
          append_body: true
          body: |
            Automated release build for `${{ github.ref_name }}`.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
